# Document Triage Workspace

Provisioned by the `workspace-foundational` Claude Code plugin (`report-parsing` variant).

Drop documents in `input/`, run `/workspace-foundational:triage-document`, read verdicts in `output/`.
