# Template Discovery Sandbox

Provisioned by the `workspace-foundational` Claude Code plugin (`template-discovery` variant).

A scratch space for deciding which cluster plugin's workspace variant best fits a new project. Invoke `/workspace-foundational:find-template` to get recommendations.
