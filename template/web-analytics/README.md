# Web Analytics Workspace

Provisioned by the `workspace-foundational` Claude Code plugin (`web-analytics` variant).

Drop raw exports in `input/raw/`, run `/workspace-foundational:analytics-ingest`, then ask analysis questions against `input/cleaned/`.
