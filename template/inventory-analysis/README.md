# Inventory Analyst Workspace

Provisioned by the `workspace-foundational` Claude Code plugin (`inventory-analysis` variant).

Drop exports in `imports/`, run `/workspace-foundational:inventory-scan`, read reports in `reports/`.
