# Generic Workspace

Provisioned by the `workspace-foundational` Claude Code plugin (`generic-workspace` variant).

Drop context into `context/`, save re-usable prompts in `prompts/`, write outputs into `outputs/`.
