# Context Maintenance Workspace

Provisioned by the `workspace-foundational` Claude Code plugin (`context-management` variant).

Manages an external RAG store via the Pinecone MCP server. Drop source material in `staging/`, log operations in `operations-log/`.
